# Core dependencies for Qwen2.5-VL transformers implementation
torch>=2.0.0
transformers>=4.37.0
qwen-vl-utils
huggingface-hub
Pillow
numpy
accelerate
bitsandbytes
safetensors
psutil

# Optional: Flash Attention for performance (skip on macOS)
flash-attn; sys_platform != "darwin"

# Optional: GGUF support (install separately if needed)
# llama-cpp-python
# For CUDA: llama-cpp-python[cuda]